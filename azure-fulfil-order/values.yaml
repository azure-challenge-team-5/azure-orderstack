# Default values for ..
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1
image:
  repository: shanepeckham/fulfillorderack
  tag: v8
  pullPolicy: IfNotPresent
service:
  type: ClusterIP
  externalPort: 80
storage:
  size: 1Gi
  # class:
# teamname:
# mongodb:
#   connectionString: 
secrets: {}
# secrets:
#   mongodb: 